function sum(num1, num2) {
    return num1 - num2;
}

sum(10, 12)

const resta = (num1, num2) => num1 - num2;

if(false) {
    console.log('Se ejecuta cuando es verdadero');
} else {
    console.log('Se ejecuta cuando es falso');
}

/**
 * Browser - no podemos utilizarlo
 * Manipulacion del DOM
 * window
 * fetch()
 * prompt()
 * alert('soy un alerts')
 */